# 汇报与理解提纲

## 一分钟介绍

我的课程项目研究如何从空间蛋白质组学分馏谱预测蛋白的亚细胞定位。我使用公开的果蝇 LOPIT 数据，以四个分馏通道的相对强度作为特征，以已有参考标记作为标签。先在训练集内交叉验证比较 SVM 和随机森林，再用独立测试集报告宏平均 F1、准确率和混淆矩阵。最后为未标记蛋白生成候选定位。结果主要体现一个可复现的分析流程，不等于实验确认新的蛋白定位。

## 可能被问到的问题

**为什么与药学相关？**

药物靶点及通路蛋白的亚细胞环境与其功能有关。这个项目练习定位分析方法，为后续分析课题组的药物扰动或疾病模型数据打基础；本项目本身没有药物处理实验。

**什么是一个样本，什么是一个特征？**

一个蛋白是一条建模样本；四个分馏通道的归一化强度是四个特征。不是把四个通道当作四个患者或四个生物学重复。

**空间信息在哪里？**

来自亚细胞分馏后的共分布模式，而不是显微图像中的 x/y 坐标。相似分布可能提示相近定位，但不能证明物理相互作用。

**SVM 和随机森林分别做什么？**

SVM 在特征空间中寻找分类边界；RBF 核允许非线性边界。随机森林综合多个决策树的预测，捕捉不同强度组合与标签的关系。

**为什么使用 Macro-F1？**

各类标记数量不同。只看准确率容易忽略少数类；Macro-F1 先算各类 F1，再等权平均。Balanced accuracy 则平均各类召回率。

**如何避免数据泄漏？**

先留出测试集。模型选择只用训练集交叉验证，SVM 的标准化在每折训练数据上拟合。测试集不用于调参；分类特征不含已有算法输出或参考标签。注意标签本身具有历史数据驱动筛选过程，这个偏差无法靠本次随机划分完全消除。

**为什么不把 unknown 当作一类？**

unknown 的意思是没有标签，并不是同一个生物学区室。它可能包含多个细胞器、复合体或多定位蛋白。

**为什么排除 Peroxisome？**

只有 4 个参考标记，难以同时支持可靠的留出评估和三折训练验证。原始记录保留，清楚声明当前模型不具备该类预测能力。

**PCA 的作用是什么？**

把四维分馏谱投影为二维，帮助观察分布和类别交叠。本项目分类器直接用四维特征，PCA 仅用于展示。

**能否把预测当作真实定位？**

不能。模型在已知类别中强制分类，对训练外类别没有拒识能力；还存在小样本、参考标签偏差和物种限制。需要独立数据与定位实验验证。

**Git 的 commit 和 tag 有什么区别？**

commit 记录一次可追踪的项目版本；tag 给某个提交命名，例如 `v1.0.0`，便于老师定位提交版本。只上传文件而不推送 tag，远程仓库不会自动出现标签。

## 提交前自己做一次

运行 `python reproduce.py`；打开 Notebook，找到模型选择结果、测试 Macro-F1、一个易混淆类别，以及至少两项局限。能解释这些内容比背诵一个准确率更重要。
